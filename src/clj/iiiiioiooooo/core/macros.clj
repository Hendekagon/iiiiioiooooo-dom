(ns iiiiioiooooo.core.macros
  (:require
    [clojure.zip :as zip]
  )
)


;(defmacro r [x] `~x)

;(defmacro add-context [s] `(assoc (second ~s) :context (zip/node (zip/down (zip/vector-zip [(zip/seq-zip (seq ~s))])))))
